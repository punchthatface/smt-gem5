# isolation_constrained_smt_gem5
gem5 simulator used for 742 research project in simulating SMT behavior.

## CloudLab setup

use Ubuntu (latest)

```bash
git clone https://github.com/punchthatface/smt-gem5.git
cd smt-gem5
./setup-cloudlab.sh

## Things to note
Make sure to change the path in config.json (the akim2) in scripts before running it.
